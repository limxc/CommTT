<UserControl x:Class="CommTT.Modules.SerialProvider.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel Orientation="Horizontal" Margin="8">
        <Button Content="Config" Command="{Binding NavigateCommand}" CommandParameter="SerialConfig"
                Margin="4,0" Style="{StaticResource MaterialDesignFlatButton}"/>
        <Button Content="Traffic" Command="{Binding NavigateCommand}" CommandParameter="SerialTraffic"
                Margin="4,0" Style="{StaticResource MaterialDesignFlatButton}"/>
        <Button Content="Monitor" Command="{Binding NavigateCommand}" CommandParameter="SerialMonitor"
                Margin="4,0" Style="{StaticResource MaterialDesignFlatButton}"/>
    </StackPanel>
</UserControl>
